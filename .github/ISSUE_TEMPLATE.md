## What happened

<!-- What did smart-test-select do, and what did you expect instead? -->

## Reproduction

- `sts` version:
- Node version:
- Command run (e.g. `sts select --base main --json`):
- Relevant part of your `tsconfig.json` (paths/aliases especially):

## Output

```
paste the output of the command here
```

## Was this a false negative?

If a test that should have run was *not* selected, mark this issue **high priority** — that is
the one failure mode this tool is not allowed to have. Please include the changed file and the
test file you expected to be selected.
